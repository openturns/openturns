# We use a longer package name so that it is more descriptive. Also avoiding
# possible troubles in the namespace of packages.
libopenturns0.20: package-name-doesnt-match-sonames libOT0.20

# Not providing symbols for the C++ library, using shlibs file instead
libopenturns0.20: no-symbols-control-file

# I could not find "boun" nor "ment" in the sources
libopenturns0.20: spelling-error-in-binary usr/lib/*/libOT* boun *
libopenturns0.20: spelling-error-in-binary usr/lib/*/libOT* ment *
